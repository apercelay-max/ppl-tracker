name: Recovery Push Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call recovery-push-check
        run: |
          curl -sf -X POST 'https://elyspjsyconovzczmzhm.supabase.co/functions/v1/recovery-push-check' \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
